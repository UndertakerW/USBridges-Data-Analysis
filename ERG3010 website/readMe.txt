The require python environment:
    python >= 3.7
    flask 1.1.2
    pymysql 0.9.3

Change the database connection information:
    change to the local database "get_mysql_cursor()" in app.pymysql

Run the file:
    1. enter in the folder of the project and run "python app.py" in terminal
    2. click the link generated in the terminal